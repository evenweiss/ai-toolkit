{
  "name": "ai-toolkit",
  "version": "0.0.1",
  "private": true,
  "description": "AI tools plugins for agentic coding",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
