# AI Toolkit

AI tools plugins for agentic coding (OpenCode, Claude Code, Cursor, etc.)

## Plugins

See [packages/](packages/) for available plugins.

## Development

```bash
pnpm install
pnpm --filter <plugin-name> build
```

