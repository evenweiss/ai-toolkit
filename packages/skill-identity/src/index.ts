/**
 * @ai-toolkit/skill-identity
 * Detect project type and set agent identity
 */

export {
  detectIdentity,
  detectProject,
  determineIdentity,
  type ProjectType,
  type ProjectInfo,
  type IdentityResult,
} from './detect';
